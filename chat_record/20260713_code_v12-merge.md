# Code Record — v1.2 Merge (2026-07-13)

## English
The live code lives in the repo (`bi-dashboard-design/scripts/*.py`, `references/charts.md` §3,
`assets/starter-template.html`). This file records the two signature pieces of the merge: the
data-driven combo (mine, replacing remote's `+520`) and the stronger `check_sync.py` (mine,
replacing remote's simple one). Remote's `check.py` and `?qa=1` harness were kept as-is (see repo).

## 中文
线上代码在仓库内。此处记录合并的两处招牌改动:data-driven 组合图(我的,替换远端 `+520`)与更强的
`check_sync.py`(我的,替换远端简版)。远端的 `check.py` 与 `?qa=1` 自检原样保留(见仓库)。

---

### 1. Data-driven combo core (charts.md §3 + template) / 组合图核心

```js
// 按序列自身值域反推轴 min/max,把线锁进纵向带 [f0,f1];替代 +520 魔法偏移。
function bandAxis(arr, f0, f1){
  const dmin=Math.min(...arr), dmax=Math.max(...arr);
  const span=(dmax-dmin)||Math.abs(dmax)||1;   // 单值/全零兜底
  const range=span/(f1-f0), min=dmin-f0*range;
  return {type:'value', show:false, min, max:min+range};
}
// yAxis:[ 柱轴 {min:0,max:v=>v.max*2}, bandAxis(rate,0.58,0.76), bandAxis(yoy,0.84,0.98) ]
// 三组柱 yAxisIndex:0;达成率线 yAxisIndex:1;同比线 yAxisIndex:2;两线 data 传真实值,label 用 {c}%
// 实测:柱顶 0.50 / 达成率带 [0.58,0.76] / 同比带 [0.84,0.98];对抗数据(0-600% / -92%..880% / 全负)全部锁带内
```

### 2. Stronger check_sync.py (two checks) / 强版同源校验(两检查)

```python
# ROOT = <skill>/scripts/ 的父目录;DOC=references/design-tokens.md;TPL=assets/starter-template.html
# 检查1 UNDEFINED-REF: §8 围栏块之外被引用的 --token 必须都在 §8 有定义(抓 --target-bar-1 vs --target-1)
# 检查2 DRIFT: §8 每个 :root/[data-theme] 块必须与模板 <style> 逐块逐值一致
_NAME = r"--[a-z][a-z0-9]*(?:-[a-z0-9]+)*"   # 不匹配 Markdown 的 ---
# 解析:剥 /* */ 注释 -> BLOCK 正则取 (selector, body) -> TOKEN_DEF 取 {token: value}
# 比较:doc_prose 的引用 ⊆ defined;doc_blocks[sel][tok] == tpl_blocks[sel][tok](小写归一)
# 运行结果:PASS — 5 块 / 42 定义一致
```

### 3. check.py graceful handling of un-asserted eval cases / check.py 对未断言用例的优雅处理

```python
allchecks = build(fname, t)
if eid not in allchecks:      # 4/5 是对抗溢出场景,无静态断言
    print(f"用例 {eid} 为对抗性溢出场景,无静态断言——请用 ?qa=1 几何自检验证(见 evals.json 的 expected_output)。")
    sys.exit(0)
checks = allchecks[eid]
```
